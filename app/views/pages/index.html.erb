<h1 class="text-center">Lista de Personajes</h1>

<div class="container mt-4">
  <div class="row">
    <% @characters.each do |character| %>
      <div class="col-md-4 mb-4">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title"><strong>Nombre:</strong> <%= character.name %></h5>
            <p class="card-text"><strong>Ubicación:</strong> <%= character.location %></p>
            <p class="card-text"><strong>Casa:</strong> <%= character.house %></p>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>